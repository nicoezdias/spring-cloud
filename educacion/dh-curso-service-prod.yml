server:
  port: ${PORT:8082}

eureka: 
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    username: sa
    password: sa
  sql:
    init.data-locations: classpath:course.sql
  jpa: 
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: create-drop

info:
  app:
    name: course-service
    description: Course Service created for Class 7
    version: 1.0.0
